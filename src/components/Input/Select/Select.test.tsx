import React from 'react';
import { shallow } from 'enzyme';

import Select from './Select';

describe('Select', () => {
  it('reminds you to write some tests', () => {
    expect(false).toEqual(true);
  });

  it('renders', () => {
    shallow(<Select>Boom</Select>);
  });
});
